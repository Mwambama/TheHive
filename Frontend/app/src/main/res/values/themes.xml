<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.HIveeApp" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Primary branding color. -->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryVariant">@color/primaryDarkColor</item>
        <item name="colorOnPrimary">@android:color/white</item>

        <!-- Secondary branding color. -->
        <item name="colorSecondary">@color/secondaryColor</item>
        <item name="colorSecondaryVariant">@color/secondaryDarkColor</item>
        <item name="colorOnSecondary">@android:color/white</item>

        <!-- Customize your status bar color here -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/primaryDarkColor</item>

        <!-- Customize your app’s background color here -->
        <item name="android:windowBackground">@color/backgroundColor</item>
    </style>
</resources>
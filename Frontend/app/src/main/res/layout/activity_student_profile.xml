<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <!-- Student Name -->
    <TextView
        android:id="@+id/profileNameLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Name:"
        android:textStyle="bold"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <EditText
        android:id="@+id/profileName"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:hint="Enter Name"
        app:layout_constraintTop_toTopOf="@id/profileNameLabel"
        app:layout_constraintStart_toEndOf="@id/profileNameLabel"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Student Email -->
    <TextView
        android:id="@+id/profileEmailLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Email:"
        android:textStyle="bold"
        app:layout_constraintTop_toBottomOf="@id/profileName"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginTop="16dp" />

    <EditText
        android:id="@+id/profileEmail"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:hint="Enter Email"
        app:layout_constraintTop_toBottomOf="@id/profileEmailLabel"
        app:layout_constraintStart_toEndOf="@id/profileEmailLabel"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="8dp" />

    <!-- Student Phone -->
    <TextView
        android:id="@+id/profilePhoneLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Phone:"
        android:textStyle="bold"
        app:layout_constraintTop_toBottomOf="@id/profileEmail"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginTop="16dp" />

    <EditText
        android:id="@+id/profilePhone"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:hint="Enter Phone"
        app:layout_constraintTop_toBottomOf="@id/profilePhoneLabel"
        app:layout_constraintStart_toEndOf="@id/profilePhoneLabel"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="8dp" />

    <!-- University -->
    <TextView
        android:id="@+id/profileUniversityLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="University:"
        android:textStyle="bold"
        app:layout_constraintTop_toBottomOf="@id/profilePhone"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginTop="16dp" />

    <EditText
        android:id="@+id/profileUniversity"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:hint="Enter University"
        app:layout_constraintTop_toBottomOf="@id/profileUniversityLabel"
        app:layout_constraintStart_toEndOf="@id/profileUniversityLabel"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="8dp" />

    <!-- GPA -->
    <TextView
        android:id="@+id/profileGPALabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="GPA:"
        android:textStyle="bold"
        app:layout_constraintTop_toBottomOf="@id/profileUniversity"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginTop="16dp" />

    <EditText
        android:id="@+id/profileGPA"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:hint="Enter GPA"
        app:layout_constraintTop_toBottomOf="@id/profileGPALabel"
        app:layout_constraintStart_toEndOf="@id/profileGPALabel"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="8dp" />

    <!-- Save Changes Button -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/saveProfileButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Save Changes"
        app:layout_constraintTop_toBottomOf="@id/profileGPA"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="24dp" />

</androidx.constraintlayout.widget.ConstraintLayout>
<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HIveeApp"
        tools:targetApi="31"
        android:usesCleartextTraffic="true">

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Company Activity -->
        <activity android:name=".company_user.CompanyActivity"
            android:exported="true">
        </activity>

        <!-- Company Add/Delete Activity -->
        <activity android:name=".company_user.AddDeleteCompanyActivity"/>


        <activity android:name=".company_user.SeeUsersActivity" />
        <activity android:name=".volley.JsonArrReqActivity" />



    </application>
</manifest>
